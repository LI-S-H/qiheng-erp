# ERP 项目 Agent 强制规则

本文件适用于所有在本仓库中工作的编码 Agent。项目沟通、文档、注释和提交信息默认使用中文。

## 前端开发强制前置流程

任何 Agent 在新增或修改以下内容之前，必须先完成前端开发预检：

- `erp-web/` 下的前端源码、组件、样式、路由、API 适配层、Mock、测试或构建配置。
- `docs/frontend-*.md`。
- 会影响前端契约的 `docs/api/erp-openapi.yaml` 或 `docs/database/` 文件。

预检命令在 `erp-web` 目录执行：

```powershell
npm run preflight:frontend -- <scope>
```

可用 scope：`system`、`product`、`purchase`、`sales`、`warehouse`、`ai`、`shared`、`all`。同时修改多个模块时传入多个 scope；修改公共组件、全局样式、路由或通用 API 时必须包含 `shared`，影响范围不明确时使用 `all`。

预检命令会完整输出前端开发规范、样式规范、对应数据库设计和相关 OpenAPI 路径，并记录文档哈希及当前 Git 提交。Agent 必须阅读输出后才能调用文件编辑工具，不得先编辑后补跑预检。

Agent 在开始编辑前必须向用户明确报告本次已完成的预检 scope。

至少必须遵守：

1. `docs/frontend-development-guide.md` 是前端开发主规范。
2. 页面字段、查询条件、响应转换必须与数据库设计和 OpenAPI 一致。
3. 一个筛选控件对应一个明确查询参数；不得使用含义不明的多字段 `keyword`。
4. 层级路径和树由前端根据扁平数据生成，后端只返回必要业务字段和可稳定关联字段。
5. 修改后必须运行契约检查、构建和对应浏览器测试。

如果规范、数据库设计和接口契约互相冲突，先停止业务代码修改，说明冲突并修正文档契约。

## 自动门禁

- Git 提交钩子会检查前端改动是否存在有效的预检凭证。
- 凭证必须基于当前 `HEAD`，必须覆盖本次改动模块，生成时间不得超过 8 小时，且规范文件哈希不能发生变化。
- 新提交产生后，下一批前端修改必须重新执行预检。
- 禁止使用 `--no-verify` 绕过钩子；紧急情况也必须由用户明确授权并记录原因。

安装仓库钩子：

```powershell
cd erp-web
npm run hooks:install
```

## 通用实现要求

- 优先遵循现有架构、命名和目录结构，做最小必要修改。
- 不覆盖或回退用户已有改动，不执行未经确认的破坏性操作。
- 先定位根因再修复；完成后报告修改内容、验证结果和剩余风险。
- 后端开发规范应单独维护，不得混入前端开发规范。
